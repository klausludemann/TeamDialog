# 11ty Pages

Each page here is a `.njk` template generated from the current HTML.
Header/footer are centralized using includes.

If you edit a page, edit the corresponding `.njk` file in this folder.
